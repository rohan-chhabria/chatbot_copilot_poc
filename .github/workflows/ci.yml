# ═══════════════════════════════════════════════════════════════════════════════
#  CI — Continuous Integration Pipeline
# ═══════════════════════════════════════════════════════════════════════════════
#
#  Every push or PR triggers:
#    1. Ruff lint
#    2. Bandit security scan
#    3. Mypy type check (non-blocking)
#    4. Pytest (unit + integration)
#    5. SAM template validation
#
# ═══════════════════════════════════════════════════════════════════════════════

name: CI — Lint & Test

on:
  push:
    branches: [main, develop, staging]
  pull_request:
    branches: [main, develop, staging]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install bandit

      - name: Lint with ruff
        run: ruff check src/ tests/ --output-format=github

      - name: Security scan with bandit
        run: |
          echo "::group::Running bandit security scan"
          bandit -r src/ --skip B101,B104,B108,B110,B311,B404,B603,B607
          echo "::endgroup::"

      - name: Type check with mypy (non-blocking)
        run: mypy src/ --ignore-missing-imports || true

      - name: Run tests
        run: pytest tests/ -v --tb=short --junitxml=test-results.xml
        env:
          AWS_DEFAULT_REGION: us-east-1
          AWS_ACCESS_KEY_ID: testing
          AWS_SECRET_ACCESS_KEY: testing
          ENVIRONMENT: test

      - name: Validate CloudFormation template
        run: |
          pip install aws-sam-cli
          sam validate --template infra/template.yaml --lint || true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-py${{ matrix.python-version }}
          path: test-results.xml
          retention-days: 7
